# GPU-focused WSL environment for MVCASyn.
# This follows the official MVCASyn README closely:
#   python 3.7 + pytorch 1.9.0 + numpy 1.19.2 + scikit-learn 1.0.2 + torch-geometric 2.0.3
# The upstream README does not pin a CUDA version, so this file uses the same
# torch 1.9.0 + cu102 stack as DualSyn to keep the environment simple on WSL.
# Create with:
#   conda env create -f benchmark_factory/env/mvc.yml
# If your ~/.condarc contains old or extra channels such as conda-forge or pkgs/free,
# create this environment from a clean HOME, for example:
#   mkdir -p /tmp/mvc_empty_home
#   HOME=/tmp/mvc_empty_home /home/$USER/miniconda3/bin/conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
#   HOME=/tmp/mvc_empty_home /home/$USER/miniconda3/bin/conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r
#   HOME=/tmp/mvc_empty_home /home/$USER/miniconda3/bin/conda env create -f benchmark_factory/env/mvc.yml
# The PyG extension wheels below are pinned to torch 1.9.0 + cu102 for linux_x86_64.

name: mvc
channels:
  - pytorch
  - rdkit
  - defaults
dependencies:
  - python=3.7
  - pip=22.3.1
  - cudatoolkit=10.2
  - pytorch=1.9.0
  - rdkit=2020.09.1.0
  - numpy=1.19.2
  - pandas=1.3.5
  - scipy=1.7.3
  - scikit-learn=1.0.2
  - pip:
      - https://data.pyg.org/whl/torch-1.9.0%2Bcu102/torch_scatter-2.0.9-cp37-cp37m-linux_x86_64.whl
      - https://data.pyg.org/whl/torch-1.9.0%2Bcu102/torch_sparse-0.6.12-cp37-cp37m-linux_x86_64.whl
      - https://data.pyg.org/whl/torch-1.9.0%2Bcu102/torch_cluster-1.5.9-cp37-cp37m-linux_x86_64.whl
      - https://data.pyg.org/whl/torch-1.9.0%2Bcu102/torch_spline_conv-1.2.1-cp37-cp37m-linux_x86_64.whl
      - torch-geometric==2.0.3
