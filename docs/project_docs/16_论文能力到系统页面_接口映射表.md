# 论文能力到系统页面_接口映射表

## 1. 编写目的

本文档用于回答“论文中的能力描述在系统中具体体现在哪里”，便于后续按章节扩写系统实现内容。

## 2. 映射表

| 编号 | 论文能力点 | 对应页面 | 对应接口 | 对应组件 | 对应目录或对象 |
| --- | --- | --- | --- | --- | --- |
| C01 | 系统总览与状态展示 | `Overview` | `/api/system/summary` | 系统总览服务 | 系统统计对象 |
| C02 | 健康检查 | 无独立页面 | `/health` | 健康检查服务 | 服务状态对象 |
| C03 | 数据集列表查看 | `Datasets` | `/api/datasets` | 数据集服务 | 数据集记录 |
| C04 | 数据集上传与登记 | `Datasets` | `/api/datasets/upload` | 数据集服务 | 上传数据包目录 |
| C05 | 统一推理任务创建 | `Inference` | `/api/inference-runs` | 推理任务服务 | 推理任务记录 |
| C06 | 推理任务列表查看 | `Runs` / `Inference` | `/api/inference-runs` | 推理任务服务 | 推理任务记录 |
| C07 | 推理任务详情与结果查看 | `Runs` / `Inference` | `/api/inference-runs/{id}` | 推理任务服务 | 推理输出目录 |
| C08 | 推理任务取消 | `Runs` / `Inference` | `/api/inference-runs/{id}/cancel` | 推理任务服务 | 推理任务状态 |
| C09 | 训练任务创建 | `Training` | `/api/training-runs` | 训练任务服务 | 训练任务记录 |
| C10 | 训练任务列表查看 | `Runs` / `Training` | `/api/training-runs` | 训练任务服务 | 训练任务记录 |
| C11 | 训练任务详情与产物查看 | `Runs` / `Training` | `/api/training-runs/{id}/detail` | 训练任务服务 | 训练版本目录 |
| C12 | 训练任务取消 | `Runs` / `Training` | `/api/training-runs/{id}/cancel` | 训练任务服务 | 训练任务状态 |
| C13 | 模型版本管理 | `Versions` | `/api/model-versions` | 版本管理服务 | 训练版本目录 |
| C14 | 统一数据包校验 | `Datasets` 间接体现 | 上传后自动触发 | 数据校验组件 | 统一数据包 |
| C15 | 模型专用输入组织 | 页面间接体现 | 训练或推理链路内部触发 | 模型输入适配组件 | 模型输入描述对象 |

## 3. 论文第 4 章使用建议

### 3.1 页面实现部分

可优先引用 `对应页面` 列，说明每个功能点的界面承载位置。

### 3.2 接口实现部分

可优先引用 `对应接口` 列，说明每个功能点的接口入口。

### 3.3 后端实现部分

可优先引用 `对应组件` 列，说明每个功能点的后端职责划分。

### 3.4 数据与目录部分

可优先引用 `对应目录或对象` 列，说明功能点最终落到的数据对象或目录位置。

## 4. 可直接引用的总结语

“从系统实现角度看，论文中的每一项核心能力都可以映射到具体页面、接口、组件和目录对象，说明系统并非停留在概念设计层面，而是具有明确的软件落点。”
