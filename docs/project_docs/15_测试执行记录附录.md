# 测试执行记录附录

## 1. 目的

本文档用于保存真实测试环境、执行步骤、执行结果以及失败项闭环记录，便于论文第 5 章和答辩展示直接引用。

## 2. 测试环境

| 项目 | 记录值 |
| --- | --- |
| 操作系统 | Windows 本地开发环境 |
| Python 版本 | 3.13.11 |
| Node.js 版本 | v24.14.0 |
| 主要验证时间 | 2026-04-28 |
| 前端访问端口 | 5173 |
| 后端访问端口 | 9000 |

## 3. 本轮执行的检查项

| 编号 | 检查项 | 结果 |
| --- | --- | --- |
| E01 | 后端健康检查接口访问 | 成功 |
| E02 | 系统总览接口访问 | 成功 |
| E03 | 前端页面端口监听检查 | 成功 |
| E04 | 后端页面端口监听检查 | 成功 |
| E05 | 训练相关目录存在性检查 | 成功 |
| E06 | 资源监控依赖检查 | 存在缺项 |

## 4. 执行步骤记录

### 4.1 健康检查

步骤：

1. 启动业务后端。
2. 访问 `/health` 接口。

结果：

接口可返回正常状态，说明后端基础服务可用。

### 4.2 系统总览检查

步骤：

1. 访问 `/api/system/summary`。
2. 观察是否返回统计信息与服务状态摘要。

结果：

接口可返回系统摘要信息，说明总览链路具备可验证性。

### 4.3 端口监听检查

步骤：

1. 检查前端访问端口是否监听。
2. 检查后端访问端口是否监听。

结果：

前端端口 `5173` 与后端端口 `9000` 均处于监听状态。

### 4.4 训练目录检查

步骤：

1. 检查训练输入包目录是否存在。
2. 检查训练版本目录是否存在。

结果：

相关目录存在，说明训练任务和训练版本的文件组织基础已经具备。

## 5. 真实结果摘要

1. 本地开发环境具备页面与接口联调条件。
2. 系统总览、健康检查和基础目录检查均有真实验证结果。
3. 训练相关目录已存在，能够支持任务记录和版本归档说明。
4. 当前更适合进行结构验证、轻量验证和文档交付验证。

## 6. 失败项与闭环记录

### 6.1 已观察到的问题

| 编号 | 问题描述 | 当前状态 | 备注 |
| --- | --- | --- | --- |
| F01 | 资源监控依赖未安装，导致部分资源信息不可展示 | 未闭环 | 不影响主要页面与接口验证 |

### 6.2 闭环记录模板

| 编号 | 发现时间 | 问题描述 | 影响范围 | 修复措施 | 验证结果 | 责任说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 示例 | 2026-04-28 | 示例问题 | 示例范围 | 示例措施 | 示例结果 | 示例说明 |

## 7. 对论文第 5 章的直接用途

1. 可直接作为测试环境表。
2. 可直接作为测试执行记录表。
3. 可直接作为限制项说明材料。

## 8. 当前说明

本文档记录的是本地真实可验证内容，强调“页面、接口、目录与任务组织可验证”，而不是将长时训练结果作为唯一测试依据。
